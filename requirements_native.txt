# RekitBox Native Minimal Requirements
Flask
pyrekordbox
mutagen
sounddevice
soundfile
# For browser-based audio, no extra Python deps needed
# For Python-native playback (optional):
# simpleaudio
# For waveform (optional, frontend):
# wavesurfer.js (add to static/ if needed)
