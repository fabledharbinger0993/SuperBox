import SwiftUI

// MARK: - FableGo Brand Colors
// Palette: Charcoal · Electric Blue · Turquoise

extension Color {
    /// Electric blue — #00AAFF — primary accent, interactive elements
    static let electricBlue  = Color("ElectricBlue")
    /// Turquoise — #00D4CC — secondary accent, highlights, progress indicators
    static let turquoise     = Color("Turquoise")
    /// Charcoal — #1C1C1E — deep background surface
    static let charcoal      = Color("Charcoal")
}
